! TYPOCTOT Defines variables required by CTODUMP and BLOKCALC containing
! values totalled over all ocean rows.

      REAL :: BUOY   ! Buoyancy conversions
      REAL :: EKTOT  ! Total KE normalised by volume
      REAL :: PLICIN ! Energy change due to implicit effects (internal)
      REAL :: PLICEX ! Energy change due to explicit effects (external)

! TYPOCTOT end
