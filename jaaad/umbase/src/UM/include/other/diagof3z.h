!
!     Offsets for diagnostics from successive calls to the radiation
!     code
!
      INTEGER, Parameter :: diagnostic_offset_sw = 100
!         offset for sw calls
      INTEGER, Parameter :: diagnostic_offset_lw = 100
!         offset for lw calls
!
